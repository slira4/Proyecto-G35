<p style="color: green"><%= notice %></p>

<h1>Products</h1>
<p>
  <%= link_to "Back", home_path %>
</p>

<%= form_with scope: 'post' do |f| %>
  <%= f.label :category, for: :product_category %>
  <%= f.select :category, ["Todos", "Bebestible", "Comestible", "Souvenir"], { include_blank: 'Elige una categoría' }, {  id: :post_category } %>
<% end %>

<div id="products">
  <% @products.each do |product| %>
    <%= render product %>
    <p>
      <%= link_to "Show this product", product %>
    </p>
  <% end %>
</div>

<%= link_to "New product", new_product_path %>
